{
  "properties": {
    "journey_id": {
      "type": "string"
    },
    "joined_date": {
      "type": "string",
      "format": "date"
    },
    "progress": {
      "type": "integer",
      "default": 0
    }
  },
  "required": [
    "journey_id"
  ],
  "name": "JourneyParticipant",
  "type": "object",
  "rls": {
    "read": {},
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}