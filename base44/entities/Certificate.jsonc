{
  "name": "Certificate",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "user_name": {
      "type": "string"
    },
    "user_email": {
      "type": "string"
    },
    "template_id": {
      "type": "string"
    },
    "template_snapshot": {
      "type": "object",
      "description": "C\u00f3pia do template no momento da emiss\u00e3o"
    },
    "certificate_type": {
      "type": "string",
      "enum": [
        "preparacao",
        "jornada",
        "renovacao"
      ]
    },
    "journey_id": {
      "type": "string"
    },
    "journey_title": {
      "type": "string"
    },
    "issue_date": {
      "type": "string",
      "format": "date"
    },
    "agreement_accepted": {
      "type": "boolean"
    },
    "agreement_text": {
      "type": "string",
      "description": "Snapshot do texto que o usu\u00e1rio concordou"
    },
    "signature_type": {
      "type": "string",
      "enum": [
        "typed",
        "uploaded"
      ]
    },
    "signature_data": {
      "type": "string",
      "description": "Nome digitado ou URL da imagem da assinatura"
    },
    "personal_data": {
      "type": "object",
      "description": "Dados preenchidos pelo usu\u00e1rio (cidade, estado, etc.)"
    },
    "pdf_url": {
      "type": "string",
      "format": "uri",
      "description": "URL do PDF unificado (certificado + termo) armazenado ap\u00f3s emiss\u00e3o"
    }
  },
  "required": [
    "user_id",
    "user_name",
    "template_id",
    "certificate_type",
    "issue_date"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "user_condition": {
        "role": "admin"
      }
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}