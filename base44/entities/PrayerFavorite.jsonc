{
  "name": "PrayerFavorite",
  "type": "object",
  "properties": {
    "prayer_id": {
      "type": "string"
    }
  },
  "required": [
    "prayer_id"
  ],
  "rls": {
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}