{
  "name": "MyriamStory",
  "type": "object",
  "properties": {
    "author_name": {
      "type": "string"
    },
    "author_photo": {
      "type": "string",
      "format": "uri"
    },
    "author_status": {
      "type": "string",
      "enum": [
        "interessado",
        "preparacao",
        "consagrado"
      ],
      "default": "interessado"
    },
    "media_url": {
      "type": "string",
      "format": "uri",
      "description": "URL da foto ou v\u00eddeo do story"
    },
    "media_type": {
      "type": "string",
      "enum": [
        "image",
        "video",
        "text"
      ],
      "default": "image"
    },
    "text": {
      "type": "string",
      "description": "Texto do story (quando tipo texto) ou legenda"
    },
    "background_color": {
      "type": "string",
      "description": "Cor de fundo para stories de texto (marian, gold, deep)",
      "default": "marian"
    },
    "viewers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": [],
      "description": "IDs de usu\u00e1rios que visualizaram"
    }
  },
  "required": [
    "media_type"
  ],
  "rls": {
    "read": {},
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}