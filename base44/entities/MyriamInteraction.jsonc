{
  "name": "MyriamInteraction",
  "type": "object",
  "properties": {
    "post_id": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "like",
        "pray"
      ],
      "default": "like"
    }
  },
  "required": [
    "post_id",
    "type"
  ],
  "rls": {
    "read": {},
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}